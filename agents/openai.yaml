interface:
  display_name: "Agent Dashboard"
  short_description: "Open a local dashboard for Codex sub-agent status and convergence checks."
  default_prompt: "Open or update the local agent dashboard for active Codex sub-agents, then run the health and convergence checks."
